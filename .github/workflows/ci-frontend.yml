name: CI · frontend

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - ".github/workflows/ci-frontend.yml"
  pull_request:
    paths:
      - "frontend/**"
  workflow_dispatch:

concurrency:
  group: ci-frontend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-deploy:
    # Self-hosted ARC runner inside the WSL cluster (see k8s/arc/) — required so the
    # SonarQube step can reach the in-cluster server. dind provides Docker for the image build.
    runs-on: cognivita-runners
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      # Vitest runs once (non-TTY). Kept non-blocking until the Angular 21 test
      # suite is stabilized — the production build below is the hard gate. Coverage is
      # emitted to frontend/coverage/lcov.info (angular.json test builder -> lcovonly) for Sonar.
      - name: Unit tests (+ coverage)
        working-directory: frontend
        continue-on-error: true
        env:
          CI: "true"
        run: npm test

      # SonarQube analysis + quality gate. This is a Node action, so it runs in the runner
      # container (which has cluster DNS) — that's why it can reach the in-cluster Sonar
      # ClusterIP. Config (projectKey, sources, lcov path, qualitygate.wait) lives in
      # frontend/sonar-project.properties.
      - name: SonarQube analysis
        uses: sonarsource/sonarqube-scan-action@v3
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL || 'http://sonarqube-sonarqube.sonarqube.svc.cluster.local:9000' }}
        with:
          projectBaseDir: frontend

      - name: Compute image tag (short SHA)
        id: meta
        run: echo "tag=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      # The multi-stage Dockerfile runs `npm ci && npm run build`, so this step is
      # also the production build gate: a broken build fails the pipeline here.
      - name: Build & push image
        uses: docker/build-push-action@v6
        with:
          context: frontend
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/cognivita-frontend:${{ steps.meta.outputs.tag }}
            docker.io/${{ secrets.DOCKERHUB_USERNAME }}/cognivita-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # The minimal ARC runner image has no `yq` (unlike GitHub's ubuntu-latest); install it.
      - name: Install yq
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL https://github.com/mikefarah/yq/releases/download/v4.44.6/yq_linux_amd64 -o "$HOME/.local/bin/yq"
          chmod +x "$HOME/.local/bin/yq"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Bump image tag in GitOps manifest
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          TAG: ${{ steps.meta.outputs.tag }}
        run: |
          set -euo pipefail
          KFILE="k8s/apps/frontend/kustomization.yaml"
          yq -i "(.images[] | select(.name == \"cognivita-frontend\")).newName = \"docker.io/${DOCKERHUB_USERNAME}/cognivita-frontend\"" "$KFILE"
          yq -i "(.images[] | select(.name == \"cognivita-frontend\")).newTag  = \"${TAG}\"" "$KFILE"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$KFILE"
          if git diff --cached --quiet; then
            echo "Manifest already up to date."; exit 0
          fi
          git commit -m "ci(frontend): deploy ${TAG} [skip ci]"
          for attempt in 1 2 3 4 5; do
            if git pull --rebase --autostash origin main && git push origin HEAD:main; then
              echo "Pushed manifest bump."; exit 0
            fi
            echo "push race — retry ${attempt}"; sleep $((RANDOM % 5 + 2))
          done
          echo "Failed to push manifest bump after retries"; exit 1
