apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
# CI (yq) keeps newName/newTag in sync with the DOCKERHUB_USERNAME secret + commit SHA.
images:
  - name: eureka-server
    newName: docker.io/borhen2/eureka-server
    newTag: feee67d
